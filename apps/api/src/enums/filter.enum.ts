export enum ReviewFilterType {
  DATE = 'DATE',
  RATING = 'RATING',
}

export enum ProductFilterType {
  ORDER = 'ORDER',
  DATE = 'DATE',
}

export enum ProductItemFilterType {
  ORDER = 'ORDER',
  DATE = 'DATE',
}

export enum ProductItemFileFilterType {
  FREE = 'FREE',
  PURCHASE = 'PURCHASE',
}
